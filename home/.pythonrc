from pprint import pprint as pp
from time import time, gmtime
from inspect import getmembers as inspect

def wut(a):
    pp(a)
    try:
        pp(a.attributes)
    except:
        print "No attributes"
    try:
        pp(a.__dict__)
    except:
        pass
